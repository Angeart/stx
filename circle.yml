machine:
  timezone: Asia/Tokyo
dependencies:
  pre:
    - cd third_party/googletest; git checkout master; cd ../../; mkdir build; cd build; cmake ..; make
test:
  pre:
    - echo "[start test]"
  override:
    - cd build; make run_test
  post:
    - echo "[end test]"
